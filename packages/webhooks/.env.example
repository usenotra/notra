# Shared Neon database. Apply the repo migration before starting this worker.
DATABASE_URL=postgresql://user:password@your-neon-host/notra?sslmode=require
# Dedicated 32-byte base64 key. Provision the same key in every producer that creates endpoints.
WEBHOOK_ENCRYPTION_KEY=
