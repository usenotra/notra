{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && bunx bun@1.4.0 install --frozen-lockfile --filter 'dashboard...' --filter turbo",
  "buildCommand": "cd ../.. && if [ \"$VERCEL_ENV\" = \"production\" ]; then bun run db:migrate && if [ -n \"$TINYBIRD_TOKEN\" ] && [ -n \"$TINYBIRD_BASE_URL\" ]; then (cd packages/analytics && bun run tinybird:deploy); fi; fi && turbo run build --filter=dashboard && echo \"=== CACHE REPORT\" && du -sh node_modules apps/dashboard/.next/cache apps/dashboard/.next/cache/* .turbo ~/.bun/install/cache ~/.cache 2>/dev/null; du -sm node_modules/.bun/* | sort -n | tail -30; ls node_modules/.bun | wc -l; df -h /vercel | tail -1",
  "regions": [
    "iad1"
  ],
  "crons": [
    {
      "path": "/api/cron/monitoring",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/cron/geo-scan",
      "schedule": "*/10 * * * *"
    },
    {
      "path": "/api/cron/daily-summary",
      "schedule": "0 8 * * *"
    }
  ]
}
